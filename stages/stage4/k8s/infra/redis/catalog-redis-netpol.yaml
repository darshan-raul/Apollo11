apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: catalog-redis-allow-specific
  namespace: apollo11-infra
spec:
  podSelector:
    matchLabels:
      app: catalog-redis
  policyTypes:
    - Ingress
  ingress:
      - from:
          - namespaceSelector:
              matchLabels:
                name: apollo11-apps
            podSelector:
              matchLabels:
                app: catalog
