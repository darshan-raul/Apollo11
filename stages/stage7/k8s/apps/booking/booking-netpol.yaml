apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: booking-allow-specific
  namespace: apollo11-apps
spec:
  podSelector:
    matchLabels:
      app: booking
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: apollo11-apps
      ports:
        - protocol: TCP
          port: 8082