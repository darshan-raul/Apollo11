# search calls flight (apps → apps).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-search-to-flight
  namespace: apollo-airlines-apps
spec:
  podSelector:
    matchLabels:
      app: flight
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: search
      ports:
        - protocol: TCP
          port: 8081
