apiVersion: v1
kind: Service
metadata:
  name: identity
  namespace: apollo-airlines-apps
spec:
  type: NodePort
  selector:
    app: identity
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30083
