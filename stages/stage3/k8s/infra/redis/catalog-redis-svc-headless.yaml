apiVersion: v1
kind: Service
metadata:
  name: catalog-redis-headless
  namespace: apollo11-infra
spec:
  clusterIP: None
  selector:
    app: catalog-redis
  ports:
    - port: 6379
      targetPort: 6379
