apiVersion: batch/v1
kind: Job
metadata:
  name: init-identity-db
  namespace: apollo11-infra
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: init-identity-db
    spec:
      containers:
        - name: init
          image: postgres:15-alpine
          command:
            - sh
            - -c
            - |
              echo "Waiting for identity-db to be ready..."
              until pg_isready -h identity-db.apollo11-infra.svc.cluster.local -U postgres; do
                echo "Waiting for identity-db..."
                sleep 2
              done
              psql -h identity-db.apollo11-infra.svc.cluster.local -U postgres -d identity -f /init/init.sql || true
              echo "identity DB init done"
          env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: apollo11-secrets
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: init-script
              mountPath: /init
      volumes:
        - name: init-script
          configMap:
            name: identity-init-script
      restartPolicy: OnFailure