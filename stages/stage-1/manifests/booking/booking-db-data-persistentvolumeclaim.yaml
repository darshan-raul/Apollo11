apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: booking-db-data
  name: booking-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
