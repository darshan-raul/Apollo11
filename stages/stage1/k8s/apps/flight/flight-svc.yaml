apiVersion: v1
kind: Service
metadata:
  name: flight
  namespace: apollo-airlines
spec:
  type: NodePort
  selector:
    app: flight
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 30081