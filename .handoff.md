# Apollo11 — Session Handoff
## Date: 2026-06-04 | Session: Apollo Airlines Transition + Docs + Stage4-5 Code

---

## Project Overview

Apollo11 is a 13-phase Kubernetes/cloud-native learning bootstrap. **Phase scope was changed** from a library management system to **Apollo Airlines** — a flight management system with 10 components (6 app services + 4 infra).

**Two repos:**
- `/home/darshan/projects/Apollo11/` — the code repo (this file lives here as `.handoff.md`)
- `/home/darshan/projects/apollo11-docs/` — the Docusaurus docs site (`npm run build` passes)

---

## What Was Built This Session

### 1. Library System Removed from Stage1 k8s Manifests
Stage1 `k8s/` originally had library-system services (auth/, catalog/, circulation/, fines/). All removed. Apollo Airlines is the only system now.

### 2. Stage4 Code Written (Go + Python + Frontend)
All code in `/home/darshan/projects/Apollo11/stages/stage4/code/`:

| Service | Language | Port | Key Features |
|---|---|---|---|
| identity | Python/FastAPI | 8080 | SIGTERM handler, `/healthz/startup`, `/healthz/live`, `/healthz/ready` |
| flight | Go/Gin | 8081 | `context.WithTimeout(30s)`, graceful SIGTERM, health probe handlers |
| booking | Go/Gin | 8082 | Same graceful shutdown pattern as flight |
| search | Go/Gin | 8083 | Same graceful shutdown pattern |
| notification | Go/Gin | 8084 | Same graceful shutdown pattern |
| frontend | Node+Go | 3000/8085 | supervisord manages nginx(3000) + probe-server(8085), multi-stage Dockerfile |

**Health probe paths (all services):**
- `GET /healthz/startup` — returns 200 during startup, 503 if failed
- `GET /healthz/live` — returns 200 if process alive (liveness)
- `GET /healthz/ready` — returns 200 only when DB connections + downstream reachability healthy, 503 otherwise

**Graceful shutdown pattern (Go services):**
```go
ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)
defer cancel()
<-sigChan
server.Shutdown(ctx)
```

### 3. Stage4 README Updated
Covers: namespaces (apollo11-infra/apps/ui), probe docs, resource limits, QoS, PodDisruptionBudget, PodPriority, graceful shutdown.

### 4. Apollo11-Docs Updated — Library → Airline
All docs in `/home/darshan/projects/apollo11-docs/docs/` updated from library system to Apollo Airlines:

**Service name mapping applied everywhere:**
- auth → identity
- catalog → flight
- circulation → booking
- fines → search

**Docs updated:** index.md, liftoff.md, stage-1.md, stage-2/{dns,services,ingress,networkpolicies,gateway-api}.md, stage-3.md, stage-5/kustomize.md, stage-6.md through stage-11.md

**Build:** `npm run build` — passes clean.

### 5. Stage5 Gaps Fixed (commit e52a8c0)
Three issues found and resolved:
1. **stage5 code missing probe handlers** — copied from stage4
2. **build-images.sh missing stage4/stage5** — copied from stage3 pattern
3. **overlays/dev/ missing stage5** — created (was referenced in README but didn't exist)

### 6. Docs Landing Page — Rocket Animation
Added to `/home/darshan/projects/apollo11-docs/src/pages/index.tsx` + `index.module.css`:
- Inline SVG rocket (no external assets)
- Diagonal trajectory: left-bottom to right-top
- 35s cycle (fast, appears immediately on load)
- 0.35 opacity, purple glow drop-shadow

---

## Apollo Airlines — Final Service Map

### App Services (6)
| Service | Language | Port | Database |
|---|---|---|---|
| identity | Python/FastAPI | 8080 | identity-db (PostgreSQL 15) |
| flight | Go/Gin | 8081 | flight-db (PostgreSQL 15) |
| booking | Go/Gin | 8082 | booking-db (PostgreSQL 15) |
| search | Go/Gin | 8083 | — |
| notification | Go/Gin | 8084 | — |
| frontend | React/Node | 3000 | — |

### Infrastructure (4)
| Service | Type | Version |
|---|---|---|
| identity-db | PostgreSQL | 15 |
| flight-db | PostgreSQL | 15 |
| booking-db | PostgreSQL | 15 |
| redis | Redis | 7 |

**Total: 10 components**

### Seed Data (present from first docker compose up)
- Airports: BOM, DEL, SIN, DXB, LHR, JFK
- Flights: AA101, AA102, AA201, AA202, AA301, AA401 (today + 30 days)
- Users: admin@apolloairlines.com/admin123 (ADMIN), passenger@apolloairlines.com/pass123 (PASSENGER)

---

## Stage Completion Status

| Stage | Status | Notes |
|---|---|---|
| Launchpad | ✅ Complete | Docker Compose, all 10 components, seed data |
| Ignition | ✅ Complete | kind cluster, first Pod, kubectl basics |
| Stage 1 | ✅ Complete | All 10 k8s Deployments + Jobs (Apollo Airlines) |
| Stage 2 | ✅ Complete | Namespaces, DNS, NetworkPolicies, Ingress (Traefik), Gateway API |
| Stage 3 | ✅ Complete | StatefulSets, PVCs, init containers, Headless Services |
| Stage 4 | ✅ Complete | Probes, resource limits, QoS, PodDisruptionBudget, graceful shutdown |
| Stage 5 | ✅ Complete | Helm charts, Kustomize overlays, GitHub Actions — gaps fixed in e52a8c0 |
| Stage 6 | ⚠️ Pending | Prometheus, Grafana, OpenTelemetry (code not written) |
| Stage 7 | ⚠️ Pending | HPA, VPA, Redis cache (code not written) |
| Stage 8-11 | ⚠️ Pending | Scope defined in AGENTS.md, not yet implemented |

---

## Namespace Naming Inconsistency (Known Issue)
Stage2/3 use `apollo-airlines-*` namespaces. Stage4+ use `apollo11-*` namespaces. This is documented but not yet aligned. Future pass should reconcile.

---

## Critical Architecture Decisions (Do Not Revert)

1. **Frontend probe server** runs on port 8085, nginx on 3000. Both managed by supervisord. k8s manifests route probes to port 8085.

2. **DATABASE_URL `***` pattern** in docker-compose.yml is intentional — `***` is a literal string in YAML, password resolves correctly in container env (not a bug).

3. **Graceful SIGTERM**: Go services use `context.WithTimeout(30s)`. Python Identity uses `signal.signal()`. Frontend uses supervisord with `TERM` signal.

4. **Docs repo is Docusaurus** (not MkDocs). Build via `npm run build`, config in `docusaurus.config.ts`, custom CSS in `src/css/custom.css`, pages in `src/pages/`.

5. **No npm on host** — frontend builds happen inside Docker multi-stage (node builds → nginx serves).

---

## Next Session Priority

### High: Stage5 Code — Apollo Airlines Names
Helm charts and Kustomize overlays in `stages/stage5/` still reference old library-system directory names (auth/, catalog/, circulation/, fines/). Need to update to Apollo Airlines service names.

### Medium: Stage6 Code — Observability
Prometheus metrics, Grafana dashboards, OpenTelemetry tracing. Code needs:
- Full `/metrics` endpoint on all services (http_requests_total, http_request_duration_ms, db_connections_active)
- OTEL SDK integrated (traces + metrics)
- trace_id/span_id propagation through all service calls
- ServiceMonitor per service for Prometheus scraping

### Medium: Stage7 Code — Caching + Scaling
Search service needs Redis caching (key: `search:{origin}:{destination}:{date}`, TTL 5min), `X-Cache: HIT/MISS` header. HPA/VPA configured in k8s but code needs to expose metrics for scaling.

### Low: Namespace Alignment
Reconcile `apollo-airlines-*` (stage2/3) vs `apollo11-*` (stage4+) namespace naming.

---

## Key Files Reference

| Path | Purpose |
|---|---|
| `Apollo11/AGENTS.md` | Full project spec, stage map, service definitions |
| `Apollo11/SPEC.md` | API contracts, DB schemas for Apollo Airlines |
| `Apollo11/README.md` | Top-level stage checklist (all 13 stages) |
| `Apollo11/stages/stage4/code/booking/main.go` | Flagship service — most complete Go example |
| `Apollo11/stages/stage4/k8s/` | All k8s manifests for stage4 |
| `apollo11-docs/docs/` | All documentation (16+ files updated to airline system) |
| `apollo11-docs/src/pages/index.tsx` | Landing page with rocket animation |

---

## Commands

```bash
# Build docs site
cd /home/darshan/projects/apollo11-docs && npm run build

# Build Apollo11 stage4 images
cd /home/darshan/projects/Apollo11/stages/stage4 && bash scripts/build-images.sh

# Deploy stage4 to kind
kubectl apply -f k8s/config/
kubectl apply -f k8s/infra/
kubectl apply -f k8s/apps/
kubectl apply -f k8s/jobs/

# Test stage4
bash /home/darshan/projects/Apollo11/test/stage4_test.sh
```

---

## Memory (for next agent)

```
Apollo11: library→airline transition complete. 10 components.
Docs site: all 16+ files updated, build passes, rocket animation on landing page.
Stage4 code: done (health probes + graceful shutdown all services).
Stage5 gaps fixed in commit e52a8c0.
Next: Stage5 code (Helm/Kustomize still have library names), then Stage6 (OTel + Prometheus).
Namespace inconsistency: stage2/3 use apollo-airlines-*, stage4+ use apollo11-*.
```